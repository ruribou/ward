ops:
  sys_uptime:
    title: 稼働状況
    description: ホストの稼働時間とロードアベレージ（uptime）を返す。
  sys_disk:
    title: ディスク使用量
    description: ホストのファイルシステム使用量（df -h）を返す。単一 SSD のため満杯は事故源——状態確認の最優先項目。
  sys_memory:
    title: メモリ使用量
    description: ホストのメモリ／スワップ使用量（free -h）を返す。
  sys_containers:
    title: 稼働中コンテナ
    description: ホストで稼働中の Docker コンテナ一覧（docker ps）を返す。
  sys_images:
    title: Docker イメージ
    description: ホストが保持する Docker イメージ一覧（docker images）を返す。
  sys_listening_ports:
    title: 待ち受けポート
    description: ホストが待ち受けている TCP/UDP ソケット一覧（ss -tuln）を返す。公開面（攻撃面）の確認に。
  sys_network:
    title: ネットワーク
    description: ホストの各インターフェースの IP アドレス一覧（ip -br addr）を返す。
  sys_failed_units:
    title: 失敗したサービス
    description: 起動・実行に失敗した systemd ユニット一覧（systemctl --failed）を返す。健全性の最初の確認に。
  sys_pull_image:
    title: イメージ取得
    description: ホストに Docker イメージを取得する（docker pull）。許可リストから選ぶ（image — hello-world / alpine / busybox / nginx のいずれか）。ホストのディスク状態を変える書き込み操作——承認が要る。取得結果は sys_images で確認できる。
    plan: 選んだイメージをホストのローカル Docker イメージストアに追加する。既にあれば更新のみで他は変わらない。sys_remove_image で巻き戻せる。
  sys_remove_image:
    title: イメージ削除
    description: ホストから Docker イメージを削除する（docker rmi）。許可リストから選ぶ（image — hello-world / alpine / busybox / nginx のいずれか）。sys_pull_image の逆＝巻き戻し操作。書き込み操作——承認が要る。
    plan: 選んだイメージをホストのローカル Docker イメージストアから削除する。無ければ何も起きない（no-op）。sys_pull_image で巻き戻せる。
  sys_container_logs:
    title: コンテナログ
    description: Docker コンテナの直近 200 行のログ（docker logs --tail 200）を返す。許可リストから選ぶ（container — web / nginx のいずれか）。read-only——サービスの動作確認や失敗原因の調査に。
  sys_run_container:
    title: コンテナ起動
    description: Docker コンテナをバックグラウンドで起動し、コンテナのポートをホストの固定ポートに公開する（docker run -d --name <name> -p <publish> <image>）。それぞれ許可リストから選ぶ（name — web；publish — 8080:80；image — nginx）。ホストの稼働状態を変える書き込み操作——承認が要る。応答しているかは sys_http_check で確認できる。
    plan: 選んだイメージからコンテナを作成・起動し、コンテナのポートを選んだ固定ホストポートに公開する（例 ホスト 8080 → コンテナ 80）。sys_remove_container で巻き戻せる。
  sys_remove_container:
    title: コンテナ削除
    description: Docker コンテナを強制削除する（docker rm -f）。許可リストから選ぶ（name — web）。sys_run_container の逆＝巻き戻し操作。書き込み操作——承認が要る。
    plan: 選んだコンテナを停止・削除する。無ければ何も起きない（no-op）。sys_run_container で巻き戻せる。
  sys_stop_container:
    title: コンテナ停止
    description: 稼働中の Docker コンテナを停止する（docker stop）。許可リストから選ぶ（name — web）。書き込み操作——承認が要る。再度起動すれば巻き戻せる。
    plan: 選んだコンテナを停止する（ホストには残り、再起動できる）。sys_start_container で巻き戻せる。
  sys_start_container:
    title: コンテナ開始
    description: 停止中の Docker コンテナを起動する（docker start）。許可リストから選ぶ（name — web）。sys_stop_container の逆。書き込み操作——承認が要る。
    plan: 選んだ停止中のコンテナを起動する。sys_stop_container で巻き戻せる。
  sys_http_check:
    title: HTTP 確認
    description: 許可リストの localhost URL に HTTP HEAD リクエストを送り、ステータス行とヘッダを返す（curl -sS -I --max-time 5 <url>；url — http://localhost:8080/ のいずれか）。read-only——sys_run_container で起動したコンテナが実際に応答している（200 か）の確認に。リダイレクトは追わないので loopback ホストから出ない。
proposal:
  notice: |-
    提案 {id} — この操作はホストを変更します（risk: {risk}）。人間が承認するまで実行されません。
    $ {command}  on {host}
    {plan}
    承認は out-of-band です。あなた自身のターミナルで実行してください:  ward approve {id}
    （保留中は ward list で確認。AI に承認ツールはありません——AI が代わりに承認することはできません。）
  plan: "plan: {description}"
  precheck: "先に確認（read-only）: $ {precheck}"
gate:
  reversible: "巻き戻し可能: この変更は {inverse} で取り消せる。"
  irreversible: "巻き戻し不可: この変更は取り消せない——逆操作はない。"
cli:
  notFound: 'ward: 承認できません — 提案 "{id}" は存在しないか、既に使用済みです。'
  noPending: 保留中の提案はありません。
  pendingHeader: "保留中の提案（承認は: ward approve <id>）:"
  discarded: 'ward: 提案 "{id}" を破棄しました。'
  metricsNoSource: "ward metrics: ログのパスを渡すか、WARD_AUDIT_LOG を設定するか、stdin にログを流してください。"
  metricsReadFailed: 'ward metrics: "{source}" を読めません — {error}'
  wait:
    usage: |-
      ward wait <id> [--timeout=<秒>] [--interval=<ミリ秒>] — 提案 <id> が人間に処理されるまでブロックする。
      read-only: 保留ストアを観察するだけで、承認はしません。承認は人間が ward approve で行います。
      既定: --timeout=600（秒）、--interval=1000（ミリ秒）。処理されたら 0、タイムアウトで 124 を返します。
    consumed: 'ward: 提案 "{id}" は保留ストアから消えました——人間が承認または却下しました。承認はあくまで人間が行い、ward wait は観察しただけです。承認が失敗しても提案は消費されるため、read-only 操作でホストが実際に変わったかを必ず確認してください（成功と決めつけない）。'
    timeout: 'ward: {timeout}秒でタイムアウトしました——提案 "{id}" はまだ保留中です（変更は行われていません）。ward wait を再実行するか、ward list を確認してください。'
    invalidId: 'ward: "{id}" は有効な提案 id ではありません（p<数字>、例: p1 を想定）。'
  usage: |-
    ward — AI が提案したインフラ変更を、人間が承認するためのコマンド。
    使い方:
      ward list             保留中の提案（と plan）を表示
      ward approve <id>     保留中の提案を承認して実行
      ward reject <id>      提案を実行せずに破棄
      ward wait <id>        人間が <id> を承認／却下するまでブロック（read-only；タイムアウトで 124）
      ward metrics [path]   監査ログを集計（成功率 / 介入率 / blast radius）
      ward config ...       ユーザ設定（language, ssh_host）の表示・変更
  config:
    usage: |-
      ward config — ~/.ward/config.yaml のユーザ設定を表示・変更する。
      使い方:
        ward config get            保存済みの設定（とファイルパス）を表示
        ward config set <key> <v>  設定を変更（key: language | ssh_host）
        ward config path           解決済みの設定ファイルパスを表示
      注意: ssh_host は別名（~/.ssh/config で解決）のみ——実 IP やホスト名は書かない。
    getHeader: "ward config — {path} の設定:"
    valueLine: "  {key} = {value}"
    unsetLine: "  {key} = (未設定 → 既定)"
    setOk: "ward config: {key} = {value} を {path} に設定しました。"
    unknownKey: 'ward config: "{key}" は設定できるキーではありません（language, ssh_host のいずれか）。'
    invalidValue: "ward config: {error}"
    path: "{path}"
