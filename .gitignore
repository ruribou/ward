# macOS
.DS_Store

# editor
*.swp
*~

# Claude Code local (personal, never commit to this public repo)
CLAUDE.local.md
.claude/settings.local.json

# secrets / local — never commit (infra repo + public: be strict)
.env
*.env
.env.*
!.env.example
!.env.sample
secrets/
secrets.yaml
secrets.yml
secrets.json
*.key
*.pem
*.crt
id_*
kubeconfig
*.kubeconfig

# infra inventory / host data — real addresses live here; keep out of this public repo
inventory
inventory.ini
inventory.yaml
inventory.yml
inventory/
hosts.ini
host_vars/
group_vars/

# IaC state / Ansible vault (tools TBD — add patterns as we pick them)
*.tfstate
*.tfstate.*
.vault_pass
vault_pass*
*.retry
*.vault
vault.yml

# logs — audit logs can carry real host network output (sys_network / sys_listening_ports)
*.log

# node / typescript
node_modules/
dist/
coverage/
*.tsbuildinfo
